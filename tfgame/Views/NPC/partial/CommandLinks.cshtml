@model tfgame.ViewModels.MindControlViewModel

@{
    if (Model.MindControl.Type == tfgame.Statics.PvPStatics.MindControl__Movement)
    {
        @Html.ActionLink("Command Movement","MoveVictim","NPC", new {id=Model.Victim.Player.Id}, null)
    }
}